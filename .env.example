# Google Search Console verification token (content value of the
# google-site-verification meta tag). Set this in Vercel → Settings →
# Environment Variables, then redeploy. Leave empty to omit the tag.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
